1. We need to add `Settings_Tooltip_RoundedCorners` to `AppStrings`.
   - Update `QuickView/AppStrings.h` to declare `extern const wchar_t* Settings_Tooltip_RoundedCorners;`
   - Update `QuickView/AppStrings.cpp` to define it and add translations for English, Chinese (Simplified & Traditional), and Japanese.
     (e.g., EN: "Controls window and context menu rounded corners. Requires Windows 11.", CN: "控制主窗口及右键菜单圆角，仅支持Windows 11。", TW: "控制主視窗及右鍵選單圓角，僅支援Windows 11。", JP: "ウィンドウとコンテキストメニューの角丸を制御します。Windows 11のみをサポートします。")
   - Initialize it at the end of `AppStrings::Initialize()` like others.
2. Move `itemRounded` from `tabVisuals` to `tabTheme` in `QuickView/SettingsOverlay.cpp`.
   - Before `tabTheme.items.push_back(itemRounded);`, add `itemRounded.tooltipText = AppStrings::Settings_Tooltip_RoundedCorners;`
   - Detect Windows 11. Since we can extract `IsWindows11()` logic, I will add a static `bool IsWindows11()` or similar helper to `SettingsOverlay.cpp` or check `GetSystemInfo()`. Actually, `GetSystemInfo()` returns a `std::wstring` string with the OS info. But I can easily recreate `IsWindows11()` using `RtlGetVersion` in `SettingsOverlay.cpp` inside `BuildMenu()`.
   - If not Windows 11, set `itemRounded.isDisabled = true;`.
3. Apply `g_config.RoundedCorners` to the context menu.
   - Open `QuickView/GeekContextMenu.cpp`.
   - In `ApplyWindowRegion()`, update `preference = g_config.RoundedCorners ? DWMWCP_ROUND : DWMWCP_DONOTROUND;`
   - Make sure `g_config` is accessible, it's defined in `EditState.h` and usually accessed via `#include "EditState.h"` and `extern AppConfig g_config;`. Need to check `GeekContextMenu.cpp` includes.
4. Pre-commit check.
